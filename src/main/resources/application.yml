server:
  port: 5050
environment: production
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/CRUD2
    username: root
    password: ciao1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: hibernate